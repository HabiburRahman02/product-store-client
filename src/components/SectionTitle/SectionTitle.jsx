
const SectionTitle = ({heading}) => {
    return (
        <div className="my-4 text-center">
            <h3 className="text-3xl text-green-600">{heading}</h3>
        </div>
    );
};

export default SectionTitle;